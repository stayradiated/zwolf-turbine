{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "publish": "npm run build && lerna publish",
    "tidy": "lerna run tidy",
    "ncu": "lerna exec -- ncu -u && lerna exec -- npm install"
  },
  "devDependencies": {
    "lerna": "^3.10.7",
    "prettier": "^1.16.1",
    "typescript": "^3.2.4"
  }
}
