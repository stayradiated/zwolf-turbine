{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "publish": "npm run build && lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.4.3",
    "prettier": "^1.15.3",
    "typescript": "^3.1.6"
  }
}
